<div class="top-row ps-3 navbar navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand fw-bold" href="">🧠 Neurabrain</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column mt-3">

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="me-2">📊</span> Dashboard
            </NavLink>
        </div>

        @* Ξεσχολιάζουμε το AuthorizeView όταν συνδέσουμε το Login *@
        @* <AuthorizeView Roles="Admin, SuperAdmin"> *@
        @* <Authorized> *@
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="users">
                <span class="me-2">👥</span> Χρήστες & Πελάτες
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="subscriptions">
                <span class="me-2">💳</span> Συνδρομές
            </NavLink>
        </div>
        @* </Authorized> *@
        @* </AuthorizeView> *@

        @* <AuthorizeView Roles="SuperAdmin"> *@
        @* <Authorized> *@
        <hr class="text-white-50 mx-3" />
        <div class="nav-item px-3">
            <NavLink class="nav-link text-warning" href="system-settings">
                <span class="me-2">⚙️</span> System Settings
            </NavLink>
        </div>
        @* </Authorized> *@
        @* </AuthorizeView> *@

    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}